import { makeStyles } from '@material-ui/core/styles';


export const useStyles = makeStyles({
    root: {
        maxWidth: 500,
        margin: '1rem',
    },
    btn: {
        margin: '10px auto',
        width: '100%'
    }
});