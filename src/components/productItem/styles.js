import { makeStyles } from '@material-ui/core/styles';


export const useStyles = makeStyles({
    root: {
        maxWidth: 500,
        margin: '0 auto',
    },
    cont: {
        minWidth: 300,
    }
});